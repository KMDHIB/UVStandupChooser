{
  "name": "uvstandupchooser",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite --open",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^6.2.2",
    "vite-plugin-pwa": "^0.21.1"
  },
  "dependencies": {
    "workbox-window": "^7.0.0"
  }
}
